import json
import os
from random import random, choice
import requests

# комементарий
def formatWord(word): # комементарий
	newWord = word
	sliceI = 0
	isPunctMark = False
	while sliceI < -len(word) + 1 and not word[-sliceI - 1].isalpha():
